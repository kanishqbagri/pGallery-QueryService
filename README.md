# pGallery-QueryService
Query Service for the kGallery

This is a simple webapp to service the API requests for the kGallery Repo.
It exposes the following end points:

/ping
/getImageData/{category}
/upload

